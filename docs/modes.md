# Inference Modes

**Stale since the TOML decommission (2026-07-28):** this file (and the table below) was
generated by reading `/etc/forge/forge.toml` live on 2026-07-23. That file no longer
exists — Config data (context, backend, extra_args, etc.) is now store-backed in the
catalog DB (`configs` table), edited live via `/api/v1/catalog/configs/*`, no restart
required. The table below has **not** been re-audited row-by-row against the DB since the
migration except where a specific investigation below says so (currently just
`gemma4-31b-mtp`) — treat every other row as needing reverification if you suspect drift.
Query current values directly: `sqlite3 /var/lib/forge/forge.db "select name,n_ctx,parallel,extra_args from configs"` (or the read-only `GET /api/v1/catalog/configs` endpoint).

> **Catalog decommission workstream (2026-08-22, live store):** `nemotron`
> (Nemotron 3 Super 120B) and `nemotron-puzzle` retired — model cards hidden
> (`models.visibility = 'hidden'`, migration 0062) and local GGUF files deleted
> (~218GB). `nemotron-lightning` (Nemotron 3.5 Lightning 30B A3B, Q4_0) added
> as a visible vulkan mode, 128K context, verified on scratch slot a3.
> `ornith-35b` now serves **Ornith 1.5** (`ornith-ai/Ornith-1.5-35B-A3B-GGUF`
> `Ornith-1.5-35B-Q8_0.gguf`); the 1.0 file/rows retired and the froggeric
> v21.3 external template override dropped (1.5's embedded chat template is
> correct). `nemotron-nano` was already removed earlier (2026-08-18).

## Full settings table (historical snapshot — live configs in catalog DB)

| Mode | Label | Context | Backend | Parallel | ctx-checkpoints | cache K/V | Threads | Port role | llama_bin |
|---|---|---|---|---|---|---|---|---|---|
| `gemma4-mtp` | Gemma 4 26B MTP | 262144 | vulkan | 1 | 16 | q8_0/q8_0 | 16 | a1 | default |
| `gemma4-mtp-nothink` | Gemma 4 26B MTP (No-Think) | 262144 | vulkan | 1 | 16 | q8_0/q8_0 | 16 | a2 | default |
| `gpt-oss-120b` | GPT-OSS 120B | 131072 | vulkan | 1 | — (default) | q4_0/q4_0 | 16 | a1 | default |
| `nemotron-lightning` | Nemotron 3.5 Lightning | 131072 | vulkan | 1 | — (default) | q8_0/q8_0 | 16 | a1 | default |
| `ornith-35b` | Ornith 1.5 35B | 262144 | **rocm** | 1 | 16 | q8_0/q8_0 | 16 | a1 | `/opt/forge/llama.cpp-kintsugi/build-rocm-new/bin/llama-server` |
| `qwen25coder` | Qwen2.5 Coder 7B | 32768 | vulkan | 4 | — (default) | q8_0/q8_0 | 16 | a1 | default |
| `qwen36` | Qwen 3.6 | 524288 | **rocm** | 1 | 16 | q8_0/q8_0 | 1 | a1 | `/opt/forge/llama.cpp-kintsugi/build-rocm-new/bin/llama-server` |
| `qwen36-mtp` | Qwen 3.6 MTP | 262144 | **rocm** | 1 | 16 | q8_0/q8_0 | 1 | a1 | `/opt/forge/llama.cpp-kintsugi/build-rocm-new/bin/llama-server` |
| `qwen38-27b` | Qwen3.8 27B | 262144 | **rocm** | 1 | 16 | q8_0/q8_0 | 16 | a1 | `/opt/forge/llama.cpp-kintsugi/build-rocm-new/bin/llama-server` |
| `qwen3coder-q6` | Qwen3 Coder Q6 | 262144 | vulkan | 1 | — (default) | q8_0/q8_0 | 16 | a1 | default |
| `swallow-32b` | Swallow 32B | 40960 | vulkan | 1 | 0 | q8_0/q8_0 | 16 | a1 | default |
| `swallow-8b` | Swallow 8B | 131072 | vulkan | 1 | 0 | q8_0/q8_0 | 16 | a2 | default |
| `carbon-8b` | Carbon 8B | 32768 | vllm | n/a | n/a | n/a | n/a | a2 | n/a |
| `hy-mt2` | Hy-MT2 30B | 16384† | vllm | n/a | n/a | n/a | n/a | a2 | n/a |
| `laguna-s-21-q4_k_m` | Laguna S 2.1 | 262144 | **rocm** | 1 | — (default) | q4_0/q8_0 | 16 | a1 | `/opt/forge/llama.cpp-poolside/build/bin/llama-server` |
| `unloaded` | Unloaded | — | — | — | — | — | — | — | — |

**`--parallel` is `1` for every llama.cpp mode as of 2026-07-23** (see "The `--parallel`
context-splitting incident" below) — except `qwen25coder` (`4`), a short-context (32K)
fast-worker mode where multiple concurrent short requests are the actual intended use,
not a single long conversation.

> **Kintsugi rebase + ROCm-10.1 cutover 2026-08-17:** the kintsugi fork
> (`rebase/reasoning-fix`) was rebased onto latest upstream master
> (`53529fc83` on `4df29be4f`, 2026-08-16) and built twice: Vulkan (`build-vulkan-new`)
> and ROCm (`build-rocm-new` against `/opt/forge/rocm-therock-10.1`, the nightly family
> that renamed 7.15→10.1 — a branding bump, libsonames are 7.16.x). Both builds were
> tested on the exact failure that device-lost qwen38-27b twice on 2026-08-16 (a ~120K-token
> cold prefill immediately after load + multi-turn cache reuse + 3× unload/reload cycles):
> **both passed, zero `ErrorDeviceLost`/ring-timeout**. **ROCm won on performance** (183 t/s
> giant-prefill vs Vulkan 155 t/s; decode tie ~15.3 t/s) — matching the standing "prefer
> ROCm when tied" tiebreaker from the Gemma4 investigation. All kintsugi-backed modes
> (qwen38-27b, qwen36, qwen36-mtp, ornith-35b) were repointed to `builds.id=10`
> (`build-rocm-new`, backend **rocm**) and each re-verified live. The old binaries
> (`build-vulkan` 9968, `build-vulkan-reasoning-fix` 9987) were removed from the catalog and
> moved aside on disk (`*-removed` dirs). `qwen38-27b-vk`/`qwen38-27b-rocm` test configs
> (23/24) remain as switchable alternatives. See `docs/investigations.md` item 13.

> **Variant cutover 2026-08-03:** `gemma4-mtp` / `gemma4-mtp-nothink` switched from the
> TrevorJS Q8_0 main + assistant-Q4_K_M draft to the
> [HauhauCS QAT Uncensored Balanced MTP](https://huggingface.co/HauhauCS/Gemma4-26B-A4B-QAT-Uncensored-HauhauCS-Balanced-MTP)
> release (Q4_K_M main + the release's own tuned `mtp-gemma-4-26B-A4B-it.gguf` draft head,
> both under `gemma4-26b-a4b-qat-hauhau-balanced-mtp/`). Measured at full 262144 context,
> solo, on the vulkan build: **decode 91 t/s vs the prior Q8_0's 68 t/s (+33%)**; prefill
> ~229 t/s vs ~251 (Q4 dequant overhead on the compute-bound path — the expected, smaller
> tradeoff). MTP draft acceptance 55-68%, comparable to the prior setup. The old Q8_0
> weights and assistant-draft files were removed from disk after the cutover; the catalog
> (`configs` 4 & 5, `artifacts` 6 & 7) was repointed in-place. Note: the release ships only
> Q4_K_M (QAT is tuned for ~4-bit — higher quants add size with no quality gain), so this is
> the **Q4_K_M** variant, not Q4_K_XL.

**`--ctx-checkpoints`**: only `swallow-32b`/`swallow-8b` set it to `0`. Every other mode
that sets it explicitly uses `16`; several (`gpt-oss-120b`, `nemotron`, `nemotron-nano`,
`nemotron-puzzle`, `qwen25coder`, `qwen3coder-q6`) don't set it at all (llama.cpp
default). **This contradicts older doc claims** ("blanket-applied `--ctx-checkpoints 0`
to all modes") that were never actually true of the live config — see `docs/pitfalls.md`
and the note at the bottom of this file.

## Special / non-default flags per mode

| Mode | Model file | mmproj | Notable extra flags |
|---|---|---|---|
| `gemma4-mtp` | `gemma4-26b-a4b-qat-hauhau-balanced-mtp/Gemma4-26B-A4B-QAT-Uncensored-HauhauCS-Balanced-Q4_K_M.gguf` | `gemma4-26b-a4b-qat-hauhau-balanced-mtp/mmproj-Gemma4-26B-A4B-QAT-Uncensored-HauhauCS-Balanced-BF16.gguf` | `--spec-type draft-mtp --spec-draft-model .../gemma4-26b-a4b-qat-hauhau-balanced-mtp/mtp-gemma-4-26B-A4B-it.gguf --spec-draft-n-max 4 --swa-full` |
| `gemma4-mtp-nothink` | `gemma4-26b-a4b-qat-hauhau-balanced-mtp/Gemma4-26B-A4B-QAT-Uncensored-HauhauCS-Balanced-Q4_K_M.gguf` | `gemma4-26b-a4b-qat-hauhau-balanced-mtp/mmproj-Gemma4-26B-A4B-QAT-Uncensored-HauhauCS-Balanced-BF16.gguf` | same as `gemma4-mtp` + `--chat-template-kwargs '{"enable_thinking":false}'` |
| `gpt-oss-120b` | `GPTOSS-120B-Uncensored-HauhauCS-Aggressive-MXFP4.gguf` | — | — |
| `nemotron` | `nemotron-super/Q5_K_L/...-00001-of-00003.gguf` (3 shards) | — | `--reasoning-budget 8192` |
| `nemotron-nano` | `nemotron-nano-omni/...UD-Q5_K_XL.gguf` | `nemotron-nano-omni/mmproj-F16.gguf` | `--reasoning-budget 8192` |
| `nemotron-puzzle` | `puzzle-75b/Q4_K_M/...-00001-of-00002.gguf` (2 shards) | — | `--reasoning-budget 8192 -ngl 999` |
| `ornith-35b` | `Ornith-1.0-35B-Q8_0.gguf` | — | `--repeat-penalty 1.08 --dry-multiplier 0.9 --temp 0.7 --chat-template-file /opt/forge/models/chat-templates/ornith-froggeric-v21.3.jinja --chat-template-kwargs '{"preserve_thinking":false}'` |
| `qwen25coder` | `qwen2.5-coder-7b-q4_k_m.gguf` | — | — |
| `qwen36` | `Qwen3.6-35B-A3B-Uncensored-HauhauCS-Aggressive-Q6_K_P.gguf` | `mmproj-Qwen3.6-35B-A3B-Uncensored-HauhauCS-Aggressive-f16.gguf` | `--chat-template-kwargs '{"preserve_thinking":true}' --reasoning-budget 8192 -b 8192 -ub 4096 --spec-type ngram-map-k` |
| `qwen36-mtp` | `Qwen3.6-35B-A3B-MTP-UD-Q4_K_XL.gguf` | `mmproj-qwen36-35b-BF16.gguf` | `--chat-template-kwargs '{"preserve_thinking":true}' --reasoning-budget 8192 --spec-type draft-mtp --spec-draft-n-max 3` |
| `qwen38-27b` | `qwen3.8-27b/Qwen3.8-27B-UD-Q8_K_XL.gguf` | `qwen3.8-27b/mmproj-BF16.gguf` | `--chat-template-kwargs '{"preserve_thinking":true}' --reasoning-budget 8192 --spec-type draft-mtp --spec-draft-n-max 3` |
| `qwen3coder-q6` | `qwen3-coder-next-q6/UD-Q6_K_XL/...-00001-of-00003.gguf` | — | — |
| `swallow-32b` | `qwen3-swallow-32b-rl-Q8_0.gguf` | — | — |
| `swallow-8b` | `Qwen3-Swallow-8B-RL-v0.2-Q4_K_M.gguf` | — | — |
| `carbon-8b` | `carbon-8b` (vLLM, safetensors) | — | `--trust-remote-code --dtype bfloat16` |
| `hy-mt2` | `hy-mt2-30b` (vLLM, safetensors) | — | `--tensor-parallel-size 1 --dtype bfloat16 --gpu-memory-utilization 0.98` |
| `laguna-s-21-q4_k_m` | `laguna-s-2.1-Q4_K_M.gguf` | — | `-np 1 --threads 16 -b 8192 -ub 4096 --reasoning-budget 8192 --spec-type draft-dflash --spec-draft-n-max 15 -md /opt/forge/models/laguna-s-2.1-DFlash-BF16.gguf` |

All llama.cpp modes also set `--no-mmap --jinja --flash-attn on` (omitted from the table
above — universal, not mode-specific).

## Service modes

Modes with `type = "service"` in `forge.toml` are standalone systemd services. They are excluded from the inference mode grid and appear in the dashboard **Services panel** with ▶ Start / ⏹ Stop buttons. Mode switching never touches them.

| Mode | Label | Unit | Notes |
|---|---|---|---|
| `creative` | ComfyUI | `forge-comfyui` | Image/video/3D generation, runs locally on ForgeHost (port 3001). PairNode runs a separate, independent ComfyUI install (port 7860) controlled via the Forge Node Agent, not this service mode — see `docs/infrastructure.md`. |

To add a new service mode, add `type = "service"` to any `[modes.<name>]` block and supply a `[[modes.<name>.services]]` entry with `unit = "<systemd-unit>"`. The dashboard picks it up automatically on the next status poll.

> **Ornith 35B** (`ornith-35b`) is a sparse MoE coding model from deepreinforce-ai (Ornith-1.0, MIT). Post-trained on Gemma 4 and Qwen 3.5 lineage. 256 routed experts / 8 active + 1 shared, 40 layers — active compute similar to Qwen3.6-35B-A3B. Strong agentic coding benchmarks (SWE-bench Verified 75.6, Terminal-Bench 2.1 64.2). 262k native context.
>
> **Ornith 1.5 cutover (2026-08-22):** `ornith-35b` now serves **Ornith 1.5**
> (`ornith-ai/Ornith-1.5-35B-A3B-GGUF`, `Ornith-1.5-35B-Q8_0.gguf`, 37.8 GB).
> Model renamed to "Ornith 1.5 35B", family renamed "Ornith 1.5", config
> `ornith-35b` repointed to the new variant/artifact (same name kept so client
> aliases stay stable). 1.0 rows + `Ornith-1.0-35B-Q8_0.gguf` retired after the
> 1.5 file was verified live (n_ctx 262144, correct completion on scratch slot
> a3). 1.5's embedded `tokenizer.chat_template` (7.8 KB, Qwen 3.5 lineage with
> thinking/vision macros) is correct — the froggeric v21.3 external template +
> `--chat-template-kwargs preserve_thinking:false` overrides (needed for 1.0's
> broken embedded template, see below) are **dropped** from the config.
>
> **Source/cutover 2026-08-03:** serving **`Ornith-1.0-35B-Q8_0.gguf`** from
> `unsloth/Ornith-1.0-35B-GGUF` (fixed chat-template build, commit `041a66d`), replacing the
> original `ornith-1.0-35b-Q8_0.gguf` from deepreinforce-ai. The old file's embedded
> `tokenizer.chat_template` (md5 `fa5eb1eee497fc8f3bccc3fcf60d1a51`) was **broken**: the
> assistant-turn branch rendered `<think>` unconditionally with no `last_query_index` guard,
> so every previous turn's reasoning was re-injected into the prompt — the model re-read
> stale plans and re-issued the same tool calls (infinite tool-call loops) on long agentic
> sessions, plus context inflation every turn. This is the byte-identical template defect
> reported in [HF discussion #31](https://huggingface.co/deepreinforce-ai/Ornith-1.0-35B/discussions/31).
> The unsloth build's embedded template (md5 `b3f24e8a3bfce199b653d70d10c9c8bf`) restores the
> `last_query_index` guard and `preserve_thinking` support.
>
> **Safe template + samplers (2026-08-03):** the mode forces **froggeric's v21.3** fixed Qwen
> template (`/opt/forge/models/chat-templates/ornith-froggeric-v21.3.jinja`) via
> `--chat-template-file` as a belt-and-suspenders override, and adds anti-repetition sampler
> defaults (`--repeat-penalty 1.08 --dry-multiplier 0.9 --temp 0.7`).
>
> **⚠ `--chat-template-kwargs '{"preserve_thinking":false}'` is REQUIRED for this model.**
> Ornith is Qwen 3.5 lineage (post-trained on Gemma 4 + Qwen 3.5), and Qwen 3.5 does **not**
> preserve historical reasoning traces (only Qwen 3.6 does — see
> [HF discussion #16](https://huggingface.co/deepreinforce-ai/Ornith-1.0-35B/discussions/16)).
> froggeric v21.3 defaults `preserve_thinking: true`, which made Ornith burn its entire token
> budget inside `<think>` with empty `content` (verified live 2026-08-03: 1500-token budget
> consumed, no answer). With `preserve_thinking: false` it completes normally. This mirrors the
> existing `qwen36`/`qwen36-mtp` modes, which set `{"preserve_thinking":true}` because **Qwen 3.6
> is the lineage that preserves thinking** — the opposite setting is correct for Ornith.
>
> **Hybrid architecture caveat:** GGUF metadata confirms `general.architecture = qwen35moe` with SSM/recurrent layers, not a pure transformer. llama.cpp cannot currently reuse cache across turns for this architecture — every turn of a long multi-turn session forces a full reprocess of the entire conversation, growing slower as it goes, which can exceed a calling agent's timeout on very long sessions. This is an upstream llama.cpp limitation, not fixable via config today — see `docs/pitfalls.md` and `docs/investigations.md` item 9. (Mitigated locally by the Kintsugi fork + `--ctx-checkpoints 16`, which is set on this mode.)
>
> **Reasoning-budget/leak fix (2026-08-04):** operator reported Ornith instability was not
> actually resolved by the 2026-08-03 template fix above — verified true. Root cause (found via
> a separate opencode investigation the same day, cross-checked here): Ornith's `<think>`
> reasoning length is highly variable (100–4300+ tokens) and was silently blowing well past
> `--reasoning-budget` (a client-supplied `max_tokens=1024` request could see reasoning alone
> consume 4200+ tokens, `finish_reason="length"`, empty `content` — not a budget cap failure
> specific to this fleet, this is a documented, industry-wide reasoning-model failure mode,
> also hit by DeepSeek R1/Gemini). The `--reasoning off` fix that opencode believed it deployed
> was **never actually applied** to the live config or binary — traced via the audit log and a
> direct `ps` check on the running process, a real gap between what an agent reports and what's
> actually live. Chose not to disable reasoning at all: Ornith's whole value is agentic
> reasoning (SWE-bench Verified 75.6), so `--reasoning off` would have defeated the point of
> running it. Instead: found the "Kintsugi" hybrid-cache patch was **living only as uncommitted
> working-tree edits on ForgeHost** (no git history, one `git checkout` from permanent loss) —
> committed it (`fcd4e598d`, backed up off-host as a git bundle), then rebased it onto llama.cpp
> `91c631b21` (base was `1d1d9a9ed`, 2026-07-10), which bundles two relevant upstream fixes:
> `99f3dc322` (server now honors per-request `reasoning_budget_tokens` — previously silently
> discarded client overrides) and `91c631b21` itself (fixes a reasoning leak specifically with
> force-opened bare `<think>` templates — directly relevant, since froggeric v21.3 is exactly
> that kind of template). Rebuilt clean, no conflicts (`build-vulkan-reasoning-fix`, version
> 9987). **Live-tested standalone before deploying:** `max_tokens=1024` still starves 6/6 (an
> independent, separate problem — the reasoning budget, 2048, is simply bigger than a small
> client `max_tokens`, so the request ceiling triggers first regardless of budget enforcement;
> needs a `max_tokens` floor in the a0 router, not yet built, tracked separately);
> `max_tokens=4096` succeeded 3/3, clean structured output, `finish_reason: stop`, no garbled
> characters (the old build's failure signature). Two-turn conversation confirmed the hybrid-cache
> patch survived the rebase (42-token prompt-eval on turn 2, not a full reprocess). Deployed via
> a new `builds` catalog row (id 8, `build_id` 2→8 on config 21) rather than overwriting
> `build-vulkan` in place, so rollback is a one-line `build_id` revert with the old binary
> untouched. Live-verified on the real `a1` port post-deploy (`n_ctx: 262144`, clean completion).
> **Not yet done:** the a0 `max_tokens` floor (still needed for real client traffic sending small
> values); `qwen36`/`qwen36-mtp` share the same old `build-vulkan` binary and would likely benefit
> from the same two fixes, but weren't tested against the new build and were deliberately left
> untouched pending their own verification pass.

> **Nemotron Nano Omni** uses Vulkan (29.2 GB, well under the 63 GB ceiling). Multimodal (vision via C-RADIOv4-H + audio via Parakeet-TDT); mmproj required. Download: `hf download unsloth/NVIDIA-Nemotron-3-Nano-Omni-30B-A3B-Reasoning-GGUF NVIDIA-Nemotron-3-Nano-Omni-30B-A3B-Reasoning-UD-Q5_K_XL.gguf mmproj-F16.gguf --local-dir /opt/forge/models/nemotron-nano-omni/`

> **Carbon 8B** (`carbon-8b`) is a genomic DNA language model (HuggingFaceBio/Carbon-8B). Architecture is standard LlamaForCausalLM, but uses a custom hybrid tokenizer (`<dna>` tag switches between BPE and 6-mer DNA encoding mid-sequence). This switching logic requires `trust_remote_code=True` and does not survive GGUF conversion — **vLLM is the only correct inference path**. Context 32k native (128k with YaRN). Deployed and verified working on ForgeHost. Guide: `docs/carbon8b-genomics.md`.

> **Hy-MT2 30B-A3B** (`hy-mt2`) is a 33-language translation MoE model (tencent/Hy-MT2-30B-A3B). 30B total / 3B active parameters. Uses `hy_v3` architecture — **not compatible with llama.cpp**. Runs via vLLM mainline v0.21.0+ (pre-built ROCm wheel works — no build from source needed). †Context capped at 16384 on ForgeHost: PyTorch `hipMalloc` is bounded at ~66 GB on Strix Halo APU; after weights (56 GB) and MoE profiling overhead (~7 GB) only ~1.8 GB remains for KV cache, insufficient for 32768 context (needs 3 GB). Unblocked when PyTorch/ROCm gains managed-memory KV allocation, or when llama.cpp adds `hy_v3` support (at which point it moves to `backend = "vulkan"`). See `docs/vllm-integration.md`. Download: `hf download tencent/Hy-MT2-30B-A3B --local-dir /opt/forge/models/hy-mt2-30b/`

> **ZAYA1-8B — hardware blocked on ForgeHost.** Zyphra MoE++ with Compressed Convolutional Attention. Requires 66 KB LDS; Strix Halo RDNA3.5 (gfx1151) has 64 KB. Incompatible with Zyphra's vLLM fork. llama.cpp support not yet in mainline. Revisit when either constraint resolves.

> **GPT-OSS 120B** (`gpt-oss-120b`) is OpenAI's open-weight model (Apache 2.0, released 2025). Despite the name it is a **sparse MoE — 117B total parameters, 5.1B active per token** (similar active compute to Qwen3.6-35B-A3B and Gemma 4 26B-A4B). Supports configurable reasoning effort (low / medium / high) via system prompt. Standout benchmark: AIME 2025 up to 97.9% at high reasoning. Weaker on general agentic benchmarks than Gemma 4 31B (BenchLM 34/100 vs 64/100). Text-only — no vision input. MXFP4 carries ~10 t/s penalty on AMD ROCm vs K-quant.

> **🛑 RETIRED (2026-08-22):** Nemotron Super and Nemotron Puzzle were
> decommissioned — model cards hidden (`models.visibility='hidden'`) and local
> GGUF files deleted (~218GB: `nemotron-super/` incl. orphan `UD-Q4_K_XL`, and
> `puzzle-75b/`). The notes below are historical only. Active Nemotron-family
> mode is now `nemotron-lightning` (Nemotron 3.5 Lightning 30B A3B, Q4_0,
> 18 GB, vulkan, 128K context).

> **Nemotron Super requires ROCm** — at 91 GB it exceeds Vulkan's ~63 GB GTT ceiling. All other modes use Vulkan for better token generation throughput (+8–12% vs ROCm).

> **Nemotron Puzzle 75B** (`nemotron-puzzle`) is a compressed Nemotron-3-Super (75B/9.3B active vs 120B/12.8B). Same hybrid Mamba2/Attention/LatentMoE architecture, same 1M context, ~2× throughput. Requires custom `puzzle-port` llama.cpp build (not yet in mainline). Download: `hf download YanissAmz/Nemotron-3-Puzzle-75B-A9B-GGUF 'Puzzle-75B-A9B-Q4_K_M-00001-of-00002.gguf' 'Puzzle-75B-A9B-Q4_K_M-00002-of-00002.gguf' --local-dir /opt/forge/models/puzzle-75b/Q4_K_M/`. ROCm only — 4-bit quants collapse to ~2.7 t/s on Vulkan. Benchmark: 19.9 t/s decode, 20 t/s generation on small prompts. Uses `-ngl 999` (all layers GPU). `--reasoning-budget 8192` for thinking channel. `llama_bin` points to `/opt/forge/llama.cpp-puzzle/build-rocm-10.1/bin/llama-server`. **Rebuilt 2026-08-17 against ROCm-TheRock 10.1.0** (was 7.15). The fork is still required: this model's GGUF stores `nemotron_h_moe.expert_used_count` as a per-layer array, and the fork's `get_key_or_arr` tolerance is the only thing current upstream (scalar-only `get_key`) accepts — mainline rejects it with "wrong type arr but expected type u32".

> **Nemotron Super measured footprint at full 1M context** (2026-06-19, live on ForgeHost): 90.24 GiB weights (3 shards) + ~5.4 GiB KV cache/compute buffers = 95.7 GiB process RSS, idle. KV cache is this small only because of the hybrid Mamba2/attention architecture (most layers carry O(1) state, not O(context) KV) plus `--cache-type-k q4_0 --cache-type-v q8_0`. System has 123 GiB RAM total — only ~11–13 GiB free with nemotron loaded solo; no headroom to run `a1`/`a2` alongside it. See `docs/pitfalls.md` for the GTT-counter blind spot this surfaced.

> **Laguna-S-2.1** (`laguna-s-21-q4_k_m`) is Poolside's 118B-A8B MoE coding model (256 experts, top-10 + 1 shared, 48 layers: 12 global + 36 SWA, 1M native context, runs at 256K). Requires the **Poolside llama.cpp fork** at `/opt/forge/llama.cpp-poolside/` — the fork adds the `laguna` architecture, DFlash speculative decoding, and a custom chat template. The fork must be built with `-DGGML_HIP_ROCWMMA_FATTN=ON` (see the Poolside fork note in `docs/pitfalls.md` for why — the hand-rolled WMMA flash-attn kernel has no gfx1151 device code; rocWMMA is the only working flash-attn path). DFlash draft model at `/opt/forge/models/laguna-s-2.1-DFlash-BF16.gguf` (2.1 GB). Measured ~9.6 tok/s decode, GPU at 47% utilization on ForgeHost. Download: `hf download poolside/Laguna-S-2.1-GGUF laguna-s-2.1-Q4_K_M.gguf laguna-s-2.1-DFlash-BF16.gguf --local-dir /opt/forge/models/`.

## The `--parallel` context-splitting incident (2026-07-23)

`--parallel N` in llama.cpp does **not** allocate N× the configured context — it
**splits** the single configured `context` value across N slots. `/props`'
`default_generation_settings.n_ctx` (what the engine reads to verify a load) reports
the **per-slot** value: `configured_context / parallel`. Confirmed against llama.cpp
source on this deployment (`tools/server/server-context.cpp`: `n_ctx_slot =
llama_n_ctx_seq(ctx_tgt)`, and `get_props`'s `default_generation_settings.n_ctx =
meta->slot_n_ctx`).

The PROFILE track's profiler (`docs/v5-profiling-benchmarks.md`) discovered this by
accident: it hard-aborts when actual `n_ctx` < configured, and flagged `gemma4-e2b` and
`nemotron-nano` (both then `--parallel 2`) as "silently reduced" — 131072 configured,
65536 actual, exactly half. That diagnosis was **half right**: the model genuinely
never got the full context per conversation (real, user-visible degradation — every
request was capped at 65536 tokens, not 131072), but the *cause* wasn't a GTT/kernel
allocation failure at all. It was working exactly as `--parallel 2` is documented to
work.

Root-caused live (loaded `gemma4-e2b` on an idle slot, read `/props`: `total_slots: 2`,
`default_generation_settings.n_ctx: 65536` — exactly `131072/2`). Not a V4→v0.5 migration
bug — `--parallel 2` for these modes is identical in the old V4 `config.toml` (still on
disk) and v0.5's `forge.toml`; `migrate_v4.go` carries `extra_args` over verbatim, and
V4's Python `_verify_model_context` (`forge/engine.py`) has the same
un-parallel-aware comparison. This false "silent GTT reduction" diagnosis predates v0.5
entirely; it just never blocked anything until the profiler's new hard-abort made it
visible.

**Operator decision:** models should load at their full trained context in a single
slot — no request-splitting, since `--parallel > 1` also costs per-request throughput
(compute split across concurrent slots instead of dedicated to one). Fixed live on the
deployment host (`systemctl kill -s HUP forge-daemon`, no restart) by setting `--parallel 1` on
all 4 modes that had `--parallel > 1`: `gemma4-e2b` (was 2), `nemotron-nano` (was 2),
`qwen3coder-q6` (was 2), `swallow-8b` (was 4) — see the corresponding fixed-live
entry in the deployment's progress log for the live-verification detail (both reported modes
reloaded post-fix, confirmed `total_slots: 1` + full configured `n_ctx` in `/props`).
`qwen25coder` keeps `--parallel 4` deliberately (short 32K-context fast-worker mode,
genuinely meant for concurrent short requests, not one long conversation).

**Caveat for future `migrate-v4` re-runs:** V4's `config.toml` was *not* updated to
match — it still has the old `--parallel` values for these 4 modes. If `forge
migrate-v4` is ever re-run from V4's config, this fix would need to be re-applied.

**Separately:** `qwen36`'s long-standing context reduction (524288→262144, documented
since 2026-05, `--parallel 1` so unaffected by the bug above) remains unexplained — a
real, un-investigated GTT/kernel issue at that context size. Do not assume the
`--parallel` fix above resolves it.

## `gemma4-31b-mtp` OOMs at full context — RESOLVED (2026-07-29)

**Root cause confirmed, fixed, deployed to the catalog DB, live-verified.** Original
diagnosis (below, kept for history) was correct: `--swa-full` was the driver, not model
size. Full resolution:

**What "changed"? Nothing — this was never actually working.** `--swa-full` was
blanket-applied to all Gemma4 modes on 2026-07-02 (`1dc65bf`) and "live-verified" —
but that verification only ever covered the smaller `gemma4-mtp-nothink` (26B MoE).
`gemma4-31b-mtp` (31B dense) was never pushed to its own full `262144`-token context
until PROFILE — a tool that didn't exist until the following week — did so for the
first time on 2026-07-24. v0.5's `mode_history` table (the only hard record of load
attempts, recording starts ~2026-07-22/23) shows the *first ever* load attempts at
full context for this mode are the two failures on 2026-07-24. This was a dormant,
always-broken config value, not a regression. The "kernel panic" that got attached to
this mode's reputation in a later (2026-07-28) sweep was a **separate, unrelated,
now-fixed bug**: `nemotron` silently launching on the vulkan binary instead of its
configured rocm backend (fixed via the `builds.backend` column + `repair-catalog-backend`,
`5b84863`) — `gemma4-31b-mtp`'s own backend was correctly `vulkan` the whole time and
was never affected by that bug; it just happened to be profiled in the same session.

**Upstream research (llama.cpp GitHub, checked 2026-07-29):** confirmed this is a real,
known, currently-unfixed memory/performance tradeoff, not a bug with a hidden patch.
- [discussions/24543](https://github.com/ggml-org/llama.cpp/discussions/24543) covers
  this *exact* model (Gemma 4 31B) hitting the same class of OOM with `--swa-full`; the
  maintainer's own words: `--swa-full` "keeps the whole history instead of the sliding
  window" — genuine full, non-windowed retention, not a bug. Same reporter: fits
  comfortably in 64GB without it.
- [issues/23978](https://github.com/ggml-org/llama.cpp/issues/23978) (`--swa-full`
  breaks KV cache quantization, ballooning to f16-equivalent size) looked like it might
  explain the *scale* of our OOM, but is a red herring for us — fixed upstream by
  [PR #23907](https://github.com/ggml-org/llama.cpp/pull/23907) (merged 2026-06-03),
  confirmed present in this deployment's build (`8bf3c1130`, 2026-07-25) via `git merge-base
  --is-ancestor`. Ruled out.
- [issues/21831](https://github.com/ggml-org/llama.cpp/issues/21831) (the general
  "forcing full prompt re-processing" bug `--swa-full` exists to work around) is still
  open as of 2026-07-28 — root-caused there as **exclusive to hybrid/recurrent
  architectures** (`qwen35moe`/Ornith, `gemma4moe`/the 26B-A4B MoE Gemma4 modes).
  `gemma4-31b-mtp` is dense (no SSM/recurrent layers, confirmed by our own crash log's
  `llama_kv_cache_iswa: using full-size SWA cache` line — the genuine-SWA code path, not
  the hybrid one) so `--swa-full` is a real, correctly-functioning fix for it, just an
  expensive one at large context.
- `--ctx-checkpoints`/`--swa-checkpoints` (confirmed via `--help` to be the *same flag*,
  just aliased) is still not a safe alternative:
  [issues/24265](https://github.com/ggml-org/llama.cpp/issues/24265) (hard-hangs on a
  Gemma SWA/hybrid model with checkpoints near the RAM limit) closed stale
  2026-07-23 with no fix, corroborating this repo's own existing caution
  (`docs/investigations.md` item 9). Do not enable it for this mode.

**The fix:** the mode's `262144` context was simply never sized to what `--swa-full`
actually costs — not a bug, an unvalidated config value inherited by copy from the other
three Gemma4 modes (all of which are smaller and/or lower-context and are fine).
Live-tested on the free `a4` slot (never evicting other slots):
- `--swa-full` **kept on** (preserves cache reuse — verified live: a follow-up turn in
  the same slot showed `prompt_tokens_details.cached_tokens: 33` for all 33 prior-turn
  tokens, with `selected slot by LCP similarity` in the log and no `forcing full prompt
  re-processing` — the flag is doing its job correctly at this size).
- `n_ctx` reduced `262144` → **`131072`** (a round number already proven safe elsewhere
  in the fleet, not a rigorously-found ceiling — a tighter binary search was
  deliberately not pursued once 131072 was validated as sufficient headroom).
- Backend: tested `vulkan` vs `rocm` (this deployment's ROCm builds are ROCm 7.15
  via TheRock, confirmed via `ldd` on both rocm build binaries — not the older ROCm 7.13
  comparison that used to favor vulkan by default). Real head-to-head generation
  benchmarks at the final config: vulkan avg 115.6 prefill / 24.4 decode tok/s; rocm 7.15
  avg 117.9 prefill / 24.3 decode tok/s — a statistical tie (run-to-run variance within
  each backend exceeded the gap between them). **Decision (explicit, from the operator):
  prefer ROCm 7.15 over Vulkan whenever performance is tied and there's no compatibility
  issue** — ROCm fails an OOM cleanly inside the process (a normal `cudaMalloc failed`
  error); Vulkan can silently OOM-kill the whole adjacent `forge-primary.service` via
  the kernel OOM-killer, which is exactly the failure mode that clouded this
  investigation for days. This is now a standing tiebreaker for future backend choices
  on this fleet, not a one-off for this mode.

**Final config (live in the catalog DB, `configs.id=2`):** `build_id=6` (`standard-rocm`,
ROCm 7.15), `n_ctx=131072`, `extra_args` unchanged otherwise (still includes
`--swa-full`, `--spec-type draft-mtp`, etc.). Not currently loaded on any slot (tested
on the free `a4` slot, then unloaded to restore it to empty).

**Still open, lower priority:**
1. The `131072` ceiling wasn't binary-searched — a tighter number (e.g. ~180-200K) may
   also fit; only worth chasing if the smaller context becomes a real constraint.
2. Re-verify `gemma4-mtp` (26B MoE, same `--swa-full`) isn't hiding the same risk — it
   *has* loaded successfully at its own full `262144` context twice since this was first
   flagged (`mode_history` ids 58, and the original 2026-07-23 load), so this is probably
   fine, just never explicitly re-confirmed as "checked, not just lucky."
3. **RESOLVED (2026-07-29, same day):** the operator reported triggering a PROFILE run on
   `laguna-s-21` that "never finished and did not display an error — it just disappeared
   from the dashboard," sitting loaded for 2h27min before being SIGKILL'd. Investigated
   and root-caused — **not a PROFILE bug and not a genuine hang.** `internal/engine`'s
   `killLingering()` (called at the end of every `Unload()`) was scoped to *all four*
   canonical slot ports instead of just the port of the slot actually being unloaded, so
   any unrelated single-slot unload elsewhere on the box would silently `SIGKILL`
   whatever was running on *every other* slot too — systemd's `Restart=` masked it as a
   brief availability blip rather than a visible failure, the same pattern as the
   original aux-service collateral-damage bug this function was already once fixed for
   (see the doc comment on `killLingering`). A routine, unrelated slot-unload call during
   the operator's PROFILE run collaterally killed it mid-request; PROFILE itself never
   had a chance to detect a hang because there wasn't one. Live-reproduced with a clean
   two-slot test on this host (load two unrelated modes on two slots, unload one, watch the
   other silently die and auto-restart), fixed (`killLingering` now takes explicit
   `ports ...int` — `Unload(slot)` passes only that slot's own port, `stopAll` passes
   every port), regression-tested (`TestKillLingeringDoesNotTargetOtherSlots`), full
   `go build/vet/test ./...` clean. See `go/internal/engine/lifecycle.go`.
4. **Two unrelated, pre-existing issues noticed while checking live slot state**, neither
   caused by this session: `a2` (`secondary` at the time, `swallow-8b`) has been down
   since a `SIGKILL` (OOM-killer signature) on 2026-07-23, never restarted; `slot_state`
   in the DB was stale for both `a2` and `a3` at the time (doesn't reflect their real down
   status) — a live instance of the exact desync class the 2026-07-21 status/state fix was
   supposed to have closed. (The `slot_state` staleness itself was fixed later the same
   session — a 5-minute background sync, see below — and the internal slot keys
   `primary`/`secondary` were renamed to `a1`/`a2` on 2026-07-29, see the slot-rename
   entry below.)

---

## Slot rename: `primary`→`a1`, `secondary`→`a2` (2026-07-29)

Forge's four inference slots have always had two names: an internal key
(`primary`, `secondary`, `a3`, `a4`) and a conceptual/display name (`a1`,
`a2`, `a3`, `a4` / dashboard labels `A1`-`A4`). `a3`/`a4` never needed a
second name because their internal key already matched their conceptual
position; `primary`/`secondary` were the historical odd ones out,
inherited from V4. Renamed for full consistency — **labels (`A1`-`A4`) and
ports (8080/8081/8087/8088) are unchanged**, only the internal identifier.

**Hard cutover, no back-compat alias** — `slot:"primary"` now 400s on
`/api/v1/load`/`/unload` and the MCP surface, same as this repo's other
precedent renames (`0003_headroom_rename.sql`, `0011_config_status_rename
.sql`). This is a single-operator system, not a multi-tenant API.

**What moved:** the catalog `slots` table (`name`/`unit` columns) plus
`slot_state`, `sched_queue.target_slot`, `reservations.bay`, and
`usage_events.slot` (migration `0014_slots_aN_rename.sql`, live in the DB
on next `forge` startup — no manual DB surgery); four Go literals
(`cmd/forge/merged_config.go`'s `PortRole` default, `internal/httpapi/
validate.go` + `internal/mcp/helpers.go`'s duplicate `validSlots` maps,
`internal/engine/engine.go`'s `Stub.Slots()` fallback); one frontend
literal (`web/src/components/ReservationModal.tsx`'s `SLOT_OPTIONS`); new
systemd unit files `forge-a1.service`/`forge-a2.service` + launcher
scripts `start-a1.sh`/`start-a2.sh` (manually installed on the deployment host, same
one-time-root-install pattern as the Headroom template unit) replacing
`forge-primary.service`/`forge-secondary.service`.

**The one real risk, verified before touching anything:** `SwitchMode()`
(`internal/engine/lifecycle.go`, used by `/api/v1/switch/{mode}`) uses
`svc.PortRole` as-is to pick the target slot — unlike `Load()`
(`/api/v1/load`), which explicitly overrides `svc.PortRole` with the
caller's chosen slot. So every `merged_config.go`-sourced mode always
targets whatever `PortRole` default is hardcoded there; that literal had
to move from `"primary"` to `"a1"` in lockstep with the DB rename, or
every `switch` call would have silently stopped placing anything.

Polkit needed no change (`forge-*.service` glob already covers the new
names). MCP tool JSON schemas needed no change (no `enum` pins the slot
value — `target_slot`/`bay` are free strings, validated at runtime by the
renamed `validSlots` maps).

---

## `gemma4-31b-mtp` OOMs at full context — likely `--swa-full`, not just size (2026-07-24, superseded above, kept for history)

PROFILE's memory-profiling run on `gemma4-31b-mtp` (31B dense, Q8_0, `context = 262144`,
`--spec-type draft-mtp` — loads a second, separate draft model alongside the main one)
failed at load. On `backend = "vulkan"` it silently OOM-killed the whole
`forge-primary.service` (kernel OOM-killer, `radv/amdgpu: Failed to allocate a
buffer`, no useful error surfaced). Switching to `backend = "rocm"` (both llama.cpp
builds confirmed via `--help` to support `--spec-type`/`--spec-draft-*` identically —
no MTP-support risk) gave a clean, explicit error instead of a silent kill:

```
ggml_backend_cuda_buffer_type_alloc_buffer: allocating 108800.00 MiB on device 0: cudaMalloc failed: out of memory
alloc_tensor_range: failed to allocate ROCm0 buffer of size 114085068800
llama_init_from_model: failed to initialize the context: failed to allocate buffer for kv cache
```

**114,085,068,800 bytes ≈ 106.2 GiB for the KV cache alone** — exceeds even ROCm's
~120 GB ceiling once weights + draft model are added. Backend reverted to `vulkan`
(its original value; ROCm bought nothing since the real number exceeds both ceilings).

**First-pass conclusion (superseded below): "context=262144 is just too large for a
31B model, reduce it."** The user correctly pushed back on this — 106 GB purely for
KV cache is implausible for a straightforward dense-attention config at this size, and
smells like a real, findable mechanism rather than an inherent architecture limit.

**The real suspect, found in this repo's own history: `--swa-full`.** This mode's
`extra_args` includes it (`docs/pitfalls.md`, `docs/investigations.md` §9). It was
added 2026-07-02 to fix a genuine, separate bug — llama.cpp's sliding-window-attention
cache eviction was forcing full prompt reprocessing every turn on Gemma4 modes
(confirmed via GGUF metadata: Gemma4 has no SSM/recurrent layers, so this is the
"genuine SWA" case, distinct from the still-unresolved hybrid/recurrent version of the
same symptom affecting `ornith-35b`/`qwen36`). `--swa-full` fixes that by disabling
SWA's memory-saving windowed eviction — forcing **full, non-windowed KV retention on
every layer** instead of Gemma's normal small-window cost for most layers. It was
**live-verified only on `gemma4-mtp-nothink`** (a smaller 26B MoE mode) and then
blanket-applied to all four Gemma4 modes without individually load-testing each one
at its own full configured context. `gemma4-31b-mtp` (the largest/densest of the
four, at the largest context, 262144) was never actually pushed to full context until
PROFILE did it here — which is consistent with the crash log's own line,
`llama_kv_cache_iswa: using full-size SWA cache`, printed immediately before the
allocation failure.

**Started but did not finish an A/B test to confirm this**: temporarily removed
`--swa-full` from `gemma4-31b-mtp`'s `extra_args` on ForgeHost, restarted, confirmed the
daemon came up healthy — then the user asked to stop profiling and move on before a
profile run was actually executed against the modified config. **Reverted `--swa-full`
back before stopping** (via the pre-edit backup, `/opt/forge/backups/
forge.toml.20260724-035324-swatest`) — ForgeHost is back to its known-good, original
state (`--swa-full` present, `backend=vulkan`), nothing left half-changed.

**Needs a follow-up session to actually finish the A/B test:**
1. Remove `--swa-full` from `gemma4-31b-mtp`'s `extra_args` in `/etc/forge/forge.toml` (backup first), `systemctl restart forge-daemon`.
2. Run PROFILE on `gemma4-31b-mtp` again and read the real KV-cache size without the flag.
3. **If it's now sane** (order of magnitude smaller — Gemma's SWA window ratio is typically ~5:1 local:global, so a large drop is expected): confirms `--swa-full` is the actual driver, not model size. Then the real decision becomes a tradeoff the earlier fix was already aware of — prompt-reprocessing correctness (`--swa-full` on) vs. being able to reach full context at all (`--swa-full` off) — not an arbitrary context cut. Options include leaving `--swa-full` off for this mode specifically (accepting the reprocessing behavior at long contexts) or finding a middle ground once `--ctx-checkpoints`'s open upstream hang bugs (`docs/investigations.md` §9) are resolved.
4. **If it's still huge even without `--swa-full`**: that would mean something else entirely is wrong — worth taking seriously as a genuine bug (bad GGUF metadata, a KV-quantization flag not applying, a llama.cpp regression) rather than assuming context size is simply the ceiling.
5. Either way, re-verify `gemma4-mtp` (26B, same `--swa-full`, same MTP pattern, smaller model) isn't hiding the same risk at its own full context — it was never profiled this session either.

Not fixed. No config left changed on ForgeHost. This needs a dedicated follow-up sprint —
don't reduce `gemma4-31b-mtp`'s context or otherwise touch this mode's config again
without doing the A/B test above first.

**Also flagged, not investigated:** the same silent-OOM-vs-explicit-error gap seen
here (Vulkan silently OOM-kills the whole daemon-adjacent service with no usable
error; ROCm at least fails cleanly) may be worth a general PROFILE-side safety net —
e.g. a pre-flight KV-cache size estimate before attempting a load, so a bad
config/flag combination fails fast with a clear message instead of a kernel OOM-kill.
Not scoped or attempted this session.

## Critical Runtime Flags
- **`--parallel`**: `1` for every mode except `qwen25coder` (`4`, intentional — see incident write-up above). Do not set to anything other than 1 for a mode meant to serve one long conversation at full context; see the incident section above for why.
- **`--ctx-checkpoints`**: only `swallow-32b`/`swallow-8b` set `0`. Everything else that sets it explicitly uses `16`; several modes (`gpt-oss-120b`, `nemotron`, `nemotron-nano`, `nemotron-puzzle`, `qwen25coder`, `qwen3coder-q6`) don't set it at all (llama.cpp default, currently 32 per `docs/investigations.md`). **Older doc claims of a blanket `--ctx-checkpoints 0` policy across all modes were never true of the live config** — verified against both the pre-migration V4 `config.toml` and the current v0.5 `forge.toml` on 2026-07-23; treat any doc that asserts otherwise as stale. Also the mechanism hybrid/recurrent models would need for cross-turn cache reuse — see `docs/pitfalls.md` before changing it; open upstream hang bugs make this unsafe to flip as of 2026-07 (`docs/investigations.md` item 9).
- **mmproj**: gemma4-mtp uses `gemma4-26b-a4b-qat-hauhau-balanced-mtp/mmproj-Gemma4-26B-A4B-QAT-Uncensored-HauhauCS-Balanced-BF16.gguf`; qwen36 and qwen36-mtp both use `mmproj-qwen36-35b-BF16.gguf` (vision confirmed working with MTP enabled on mainline). **Vision only** — mmproj is an image/video-frame projector with no audio encoder; video input is sampled as frames and the audio track is never read. None of these modes can be used for STT.
- **MTP flags** (`qwen36-mtp`): `--spec-type draft-mtp --spec-draft-n-max 3 --parallel 1`. Requires a GGUF with embedded MTP head tensors. Merged into mainline llama.cpp (PR #22673, 2026-05-16). **Confirmed ~79 t/s (1.7× production qwen36) on this host 2026-05-19.** The HauhauCS abliterated Q6_K_P has no MTP tensors; this mode uses the stock GGUF. Download: `hf download havenoammo/Qwen3.6-35B-A3B-MTP-GGUF Qwen3.6-35B-A3B-MTP-UD-Q4_K_XL.gguf --local-dir <models dir>`
